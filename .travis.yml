language: generic 
sudo: required
services:
    - docker

before_install:
    - docker build -t khumbu24/docker-react -f Dockerfile.dev .
    
script:
  - docker run -e CI=true khumbu24/docker-react npm run test